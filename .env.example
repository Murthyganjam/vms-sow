# Database — local Docker or Neon (get Neon URL from dashboard; use ?sslmode=require for Neon)
# Docker: postgresql://postgres:postgres@localhost:5434/vms_sow
# Neon:   postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/vms_sow"

# NextAuth (credentials provider for dummy users)
# Must match the URL you open in the browser (e.g. http://localhost:3004)
NEXTAUTH_URL="http://localhost:3004"
NEXTAUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# OpenAI — for AI-assisted SOW drafting
OPENAI_API_KEY=sk-...
